# 代码检查报告 · 第二轮（修复复验 + 新发现）

> **第一轮报告**（3 个 P0 + 8 个 P1 + 13 个 P2 的完整证据）见 git 历史：
> 原始版 `ba95c36`，带修复标注版 `08db1fc`。本文件是**第二轮**：对 `5456d5e` / `08db1fc`
> 两个提交的修复做**独立复验**，并记录本轮新发现的问题。
>
> 本文件保留第一轮的编号（`H-1`/`H-2`/`H-3`/`M-1`…`M-8`/`L-1`…`L-13`），
> 因为 `api-design.md`、`v1-scope.md` 与若干 `.cj` 注释都用这些编号做交叉引用。
>
> 写法提醒：本文件是**用文件工具写入的**，没有经过 PowerShell 双引号字符串。
> 用 shell 字符串编辑 markdown 会把 `` `r `` / `` `a `` / `` `t `` / `` `n `` 当成转义序列，
> 造成静默内容损坏 —— 见 N-2。

| 项 | 值 |
| --- | --- |
| 复验对象 | `5456d5e`（修 3 个 P0 + 21 项其它）与 `08db1fc`（同步文档与进度数字） |
| 复验方式 | 从提交源码**重新编译**，用第一轮的原始复现步骤**独立重打真实 HTTP**（不使用被复验方的测试作为证据） |
| 结论 | 原 24 条**全部确认修复**，无虚假声明；新发现 9 条（1 条中低、5 条低、3 条提示） |
| 复核方改动 | **未修改任何文件**，`git status` 全程干净 |

---

## 一、本轮做了什么

1. **重编译**：删除 `build/club-server.exe`，用 `server/build.ps1` 从提交源码重建 → 编译通过。
   这一步同时证明"提交的源码"就是"产出二进制的那份源码"。
2. **重跑三套测试**：单测 / HTTP 冒烟 / TLS 关卡，见 §2.1。
3. **独立复现**：另起 4 轮独立实例（独立数据目录 + 独立端口），用第一轮报告里**我自己写的**
   复现请求逐条重打，覆盖 H-1/H-2/H-3/M-1/M-2/M-3/M-6/M-7/M-8/L-1～L-9。
4. **回归面检查**：专门验证"修复有没有收紧过头"（正常流程是否还能走通）。
5. **向后兼容检查**：旧版 `db.json`（含越界 `sort` 与已废弃的 `login_fail` 字段）能否启动。
6. **污染扫描**：对两个提交的**全部 24 个变更文件**做控制字节扫描，确认源码与脚本零污染（见 N-2）。

---

## 二、修复复验（原 24 条）

### 2.1 三套测试基线（自行重编译 + 重跑）

| 套件 | 修复前 | 提交声明 | **我的实测** | 结论 |
| --- | --- | --- | --- | --- |
| 单测 `club-server.exe test` | 233 / 0 | 273 / 0 | **PASS 273 / FAIL 0** | 属实 |
| HTTP 冒烟 `tests/smoke.ps1` | 280 / 0 | 325 / 0 | **PASS 325 / FAIL 0** | 属实 |
| TLS `tests/tls-check.ps1` | 未跑 | 22 / 0 | **PASS 22 / FAIL 0** | 属实（首轮我误判，见附录 B） |

> 单测 +40、冒烟 +45 条里，我抽查的关键用例**确实会因回退而变红** ——
> 特别是 H-1 那条把部长临时调进主席团再断言 `FORBIDDEN_ROLE` 的用例，
> 它修掉了第一轮"403 来自 `FORBIDDEN_NOT_IN_DEPT`、角色维度根本没被测到"的假绿灯。
> 这是本轮最值得肯定的改动：**修漏洞的同时把假绿灯换成了真断言。**

### 2.2 逐条独立复验结果

> 每条都是我用第一轮的原始请求重打的真实 HTTP，"我的独立实测"列为观测值。

| ID | 修复要点 | 我的独立实测 | 结论 |
| --- | --- | --- | --- |
| **H-1** | `perms.cj` 加 `roleRank`/`outranks`，部长/副部长只能动档位更低的人 | 同部门部长重置会长密码 → `403 FORBIDDEN_ROLE`；改会长姓名 → `403`；重置副会长 → `403`；改**同档**另一位部长 → `403`；改本部门普通成员 → `200`；改自己姓名 → `200`；会长姓名确认未被改动 | ✅ |
| **H-2** | 幂等键改为 `(kind, actorId, token)` + 命中回放前复判读权限 | 外部门部长重放会长的 `client_token` → `201` 且拿到**自己的**新任务（不是回放、未泄露标题）；**第二层防御单独可验**：该部长先用 `CT2` 建任务，被调离原部门后重放 `CT2` → `403`，原任务仍在 | ✅ |
| **H-3** | 「提升为顶层」分支补判目标部门权限 + 跨部门一致性 | 部长把课题提升到别的部门 → `403` 且 `parent_id` 未变；提升到本部门 → `200`；会长跨部门 → `400 VALIDATION_FAILED` | ✅ |
| **M-1** | 新增 `planStatsOf()`，四处统一取真实聚合值 | `PATCH /plans` 的 `progress.done`/`progress.total`/`task_count`/`child_count` 与 `GET` 完全一致；幂等回放也带真实 progress | ✅ |
| **M-2** | 口令下限 4→6；注册失败全局节流 | 5 位 → `400`；6 位 → `200`；连续错口令第 11 次出现 `429` | ✅（新取舍见 N-6） |
| **M-3** | 失败不再落盘、加手机号形态校验、超 500 条清理 | `db.json` 内已无 `login_fail` 字段；非法手机号 → `400 VALIDATION_FAILED`；锁定期内仍被拒 | ✅ |
| **M-4** | 行为保留，审计按 `restore-member` 单独记账 | 恢复已退出成员后审计日志出现 `restore-member`，普通分配仍是 `assign-member` | ✅ |
| **M-5** | `timeout(15000)` 移到 `router.middleware()` 之前 | 代码位置与 `compose.cj` 洋葱语义一致（本轮无法用慢接口直接观测，属结构确认） | ✅ |
| **M-6** | `parseIso` 接受并截断小数秒 | `2026-09-14T15:24:08.307Z`（`toISOString()` 原样）→ `200`；`.Z`（无数字）→ `400` | ✅ |
| **M-7** | 装载后逐表比对条数，不等则拒绝启动 | **旧库**（越界 `sort` + 旧 `login_fail` 字段）能正常启动，`GET /depts` → `200`；改成真坏一条记录 → **拒绝启动**并打印"只解析出 N 条" | ✅（唯一例外见 N-5） |
| **M-8** | 写入侧校验 `0..10000`，读取侧 `sortKeyClamp` 兜底 | `sort = Int64 最大值` → `400`，之后 `GET /depts` 仍 `200`；旧库里的越界值经 clamp 可正常排序，且能改回合法值自救 | ✅ |
| **L-1** | `set_role` 改为从 `can()` 推导 | 副会长 `set_role=true` 且 `PATCH /members` 改角色确实 `200`；部长/成员 `false` | ✅ |
| **L-2** | 新增公开落地页路由 | `GET /join/{token}` → `200` HTML 含部门名；失效 token → `200` 提示已失效 | ✅（新缺陷见 N-1） |
| **L-3** | 不存在时也返回完整同形状视图 | 停用不存在的链接 → `200`，`token` 回显、`enabled=false`、`dept` 为空对象 | ✅ |
| **L-4** | 删死分支；口径统一为"管理员救济动作，不含本人" | 成员对自己重置 → `403 FORBIDDEN_ROLE`；完整救济链路（管理员重置 → 临时密码登录 → `PUT /auth/password` → 新密码登录）全部 `200` | ✅（措辞瑕疵见 N-7） |
| **L-5** | 加 `daysInMonth`/`isLeapYear` | `2026-02-31` → `400`；闰年 `2024-02-29` → `200`；平年 `2026-02-29` → `400`；`2026-02-28` → `200` | ✅ |
| **L-6** | 新增 `constantTimeEq` | 单测覆盖相等/首位不同/末位不同/长度不同/空串/中文；语义与 `==` 一致 | ✅ |
| **L-7** | v1 保持现状并写明取舍 | `storeSave` 顶部有完整的取舍说明与 v2 方向；未假装做过并发实测 | ✅（按要求仅记录） |
| **L-8** | 四条路径拆成 `MEMBER_PENDING` / `MEMBER_DISABLED` | 给已退出成员建任务 / 转交 → `403 MEMBER_DISABLED`；给 `pending` 成员 → `MEMBER_PENDING` | ✅ |
| **L-9** | `audit()` 写入口统一清洗控制字符 | 代码为唯一写入口，CR/LF/TAB → 空格、其它控制字符 → `?` | ✅ |
| **L-10** | `planPath` 改用 `PLAN_WALK_GUARD` | 两处魔数已统一 | ✅ |
| **L-11** | README 接口计数改为 `39 / 39` 并写明口径 | README/HANDOFF/server-guide 三处已一致 | ✅ |
| **L-12** | 三处如实说明客户端仍是模板 | README 顶部、进度表、未决事项 #7 均已写明 | ✅ |
| **L-13** | 按结论保留不改 | 未改动，符合约定 | ✅（不变） |

### 2.3 关键条目的原始实测输出

H-1（把部长放进**会长所在部门**再打 —— 第一轮就是这里被假绿灯放过的）：

```
同部门部长重置会长密码       -> 403 FORBIDDEN_ROLE
同部门部长改会长姓名         -> 403 FORBIDDEN_ROLE
同部门部长重置副会长密码     -> 403
同部门部长改另一位部长姓名   -> 403（同档）
同部门部长重置另一位部长密码 -> 403（同档）
部长改本部门普通成员姓名     -> 200（未过度收紧）
部长重置本部门普通成员密码   -> 200（未过度收紧）
会长姓名确实未被改动
```

H-2 第二层（防御纵深，可单独验证）：

```
部长用 CT2 在运营部建任务          -> 201
同一人同一地点重放 CT2             -> 200 duplicated（正常幂等）
会长把该部长调到宣传部             -> 200
换部门后重放同 CT2                 -> 403（命中回放前的权限复判拦住）
也未泄露原任务标题
原任务仍然存在（只是该部长看不到）  -> 200
```

M-7 向后兼容与反向验证：

```
旧库（越界 sort + 旧 login_fail 字段）仍能启动 -> /health 200
越界 sort 的旧库 GET /depts                   -> 200（clamp 生效）
把越界值改回合法值（自救路径）                 -> 200
真坏一条记录（members 里混字符串）             -> 进程拒绝监听
启动日志给出明确原因                           -> "只解析出 N 条"
```

### 2.4 回归面确认（"没改坏什么"）

专门验证修复没有收紧过头：

- 部长仍能改 / 重置**本部门普通成员**（`200`）；
- 部长仍能改**自己**的姓名（`200`，`is_self` 例外生效）；
- 部长仍能把课题提升为**本部门**顶层（`200`）；
- 会长仍能重置包括自己在内的任何人（`200`）；
- 完整救济链路可用：管理员重置 → 临时密码登录 → `PUT /auth/password` 改密 → 新密码登录；
- 六个列表接口全部 `200`：`/depts`、`/members`、`/tasks`、`/plans`、`/members/pending`、`/dept-invite-links`；
- 四个 `.ps1` 脚本的 **UTF-8 BOM 都在**（对应 `API-NOTES` 第 26 条）。

### 2.5 关于"修复中额外发现"的两点校订

修复方在进度表下补了「修复中额外发现并处理的问题（报告未列）」，其中：

| 项 | 校订 |
| --- | --- |
| L-8 不止两处：`PATCH /plans` 换负责人那条也把 `disabled` 报成 `MEMBER_PENDING` | **确实是新发现**，第一轮只列了 `h_task.cj:361` 与 `h_plan.cj:289` 两处 |
| M-1 不止 `PATCH`：`POST /plans` 的创建响应与幂等回放也传了 `None` | **第一轮报告已列**（M-1 的证据里写了 `h_plan.cj:165` 幂等回放、`h_plan.cj:237` 创建分支），严格说不是"报告未列" |
| 局部编辑 `smoke.ps1` 会丢 BOM | **确实是新发现**，值得记进 `API-NOTES` |

---

## 三、本轮新发现

> 修完一条把 `[ ]` 改成 `[x]`，并在各条的「状态」处补结论。

- [x] N-1 `GET /join/{token}` 落地页未转义部门名（**已加 `htmlEscape`**）
- [x] N-2 用 shell 双引号字符串编辑 markdown 会静默损坏内容（**已补成 `API-NOTES` 第 27 条**）
- [x] N-3 `api-design.md` 的 `### GET /join/{token}` 插进了 `DELETE` 小节中间，章节结构错位（**已归位**）
- [x] N-4 `README.md` 的冒烟计数写 `318`，实际与其它三处文档都是 `325`（**已改 325**）
- [x] N-5 `idem` 表是 8 张表里唯一没走 `checkLoaded` 的（**已补上，8 张表口径统一**）
- [x] N-6 注册口令节流是**全局锁**，可被用来长期挡住正常注册（**已改为按 IP + 递增退避，且不必新增接口**）
- [x] N-7 "本接口不能对自己用"对**会长**不成立（**已拦进 `checkAccess` 动作语义层**）
- [x] N-8 `sortKeyClamp` 会把不同的越界值夹成同一档，排序退化为按 id（**按约定不改**）
- [x] N-9 落地页没有任何安全响应头（**已加一行 CSP**）

### N-1 `GET /join/{token}` 落地页未转义部门名

**状态**：**已修（2026-09-14）**——`strx.cj` 新增 `htmlEscape()`，`joinPageHtml` 对 title / tip 一律先转义；同时按 N-9 补了 CSP 响应头做纵深防御。冒烟里加了两条会因回退而变红的断言（回退版实测 FAIL，页面里原样出现 `<script>alert(1)</script>`）

**现象**（修复前）：L-2 新增的落地页是本项目**第一处 HTML 输出**，但把部门名直接拼进了 HTML。
部门名只校验非空，因此一个含标签的部门名会被原样输出。

**证据**

- `server/src/h_link.cj` —— `joinPageHtml` 用字符串拼接构造页面，`title` / `tip` 未做任何转义：
  ```cangjie
  func joinPageHtml(title: String, tip: String): String {
      return "<!DOCTYPE html>...<title>${title}</title></head>" +
          "..." + "<h2>${title}</h2><p>${tip}</p>" + ...
  }
  ```
- 调用处把部门名拼进标题：`joinPageHtml("加入「${deptName}」", ...)`
- `server/src/h_dept.cj` —— 部门名只校验 `isBlank`，无字符白名单

**实测**

```
POST /api/v1/depts { name: "<script>alert(1)</script>" }   -> 201（被接受）
POST /api/v1/dept-invite-links { dept_id: <该部门> }        -> 201
GET  /join/{token}                                         -> 200
     页面中原样出现 <script>alert(1)</script>（未转义）
```

**影响：中低** —— 理由要写清楚，避免高估或低估：

- 能设置部门名的只有**会长**（`POST/PATCH /depts` 会长独占），所以这不是外部攻击者可控的注入；
  现实场景是"会长自己的误操作"，或将来某个批量导入功能写入脏数据；
- 该 origin 用 `Authorization: Bearer` 头鉴权、**不使用 Cookie**，浏览器里的脚本拿不到会话令牌，
  因此当前拿不到"接管账号"的收益；
- 但落地页是**公开、未鉴权、与 API 同源**的页面，被注入后可做篡改 / 钓鱼；
  **一旦将来在同源加任何浏览器管理界面，这一处立刻升级为真 XSS。**

**建议修法**

1. 在 `joinPageHtml` 里对 `title` / `tip` 做 HTML 转义（`&` `<` `>` `"` `'` 五个字符），
   或对 `deptName` 先过一层字符白名单（部门名本来就该是中文 / 字母 / 数字 / 少量符号）。
2. 顺带加 N-9 的 CSP —— 即使漏了转义，`default-src 'none'` 也能让注入的脚本不执行。

### N-2 用 shell 双引号字符串编辑 markdown 会静默损坏内容

**状态**：**已随本次替换消除**（旧文件已删除、本文件由文件工具写入）；**已修（2026-09-14）**——按建议补成 `API-NOTES` 第 27 条。成因仍留档如下

**现象**：第一轮报告被局部编辑后，旧 `docs/code-review.md` 里出现了 **2 个裸 BEL 字节（0x07）**
与 **3 行残缺的重复片段**，肉眼看起来像"编辑时手滑"，实际是 PowerShell 双引号字符串的转义。

**当时的证据（已随替换消失，留档）**

| 位置 | 内容 | 成因 |
| --- | --- | --- |
| L82 | `oleRank / outranks，部长与副部长…`（上一行 `**状态**：…` 的残缺重复） | `` `roleRank `` 里的 `` `r `` |
| L376 | `estore-member 与普通 <BEL>ssign-member 分开记账，并在 <BEL>pi-design §3.3 成文…` | `` `r ``、`` `a `` ×2 |
| L417 | `outer.middleware() **之前**（外层）才会真正执行…` | `` `router.middleware() `` 里的 `` `r `` |

旧文件中存在 2 个裸 BEL 字节，精确位于字节偏移 **22337** 与 **22373**。

**成因（关键）**：PowerShell **双引号**字符串里，反引号是转义引导符：

| 写法 | 变成 | 后果 |
| --- | --- | --- |
| 反引号 + `r` | CR (0x0D) | 被当成换行 → 该行被拆成两行，反引号与首字母丢失 |
| 反引号 + `a` | BEL (0x07) | 不可见控制字符留在文件里，反引号与首字母丢失 |
| 反引号 + `t` | TAB (0x09) | 行内多出一个制表符 |
| 反引号 + `n` | LF (0x0A) | 同样把行拆断 |

markdown 里 `` `restore-member` ``、`` `assign-member` ``、`` `api-design` ``、`` `router.middleware()` ``
这类代码跨度**正好全部命中**。这与 `API-NOTES` 第 26 条（局部编辑丢 BOM）是同一族问题：
**都是"用 shell 处理文本文件"引入的静默损坏。**

**扩散范围（已确认没有扩散）**：对两个提交的**全部 24 个变更文件**做了控制字节扫描
（查找孤立 CR、BEL、BS、VT、FF、NUL）与 `.cj` 裸 TAB 扫描 ——
**只有 `docs/code-review.md` 中招，服务端源码、测试脚本、其它文档全部干净。**

**建议**

1. 新增 `API-NOTES` 第 27 条：**不要用 PowerShell 双引号字符串处理 `.md` / `.cj` / `.ps1` 内容**；
   要么用文件编辑工具，要么用**单引号**字符串（单引号里反引号不是转义符）。
2. 提交前自检（下面这段请用文件工具或**单引号**写入，否则会自伤）：

   ```powershell
   $p = 'docs/code-review.md'
   $b = [System.IO.File]::ReadAllBytes((Resolve-Path $p))
   $bad = 0
   for ($i = 0; $i -lt $b.Length; $i++) {
       if ($b[$i] -lt 32 -and $b[$i] -ne 10 -and $b[$i] -ne 9 -and $b[$i] -ne 13) { $bad++ }
   }
   if ($bad -gt 0) { Write-Host "发现 $bad 个非法控制字符" }
   ```

### N-3 `api-design.md` 的 `### GET /join/{token}` 插进了 `DELETE` 小节中间

**状态**：**已修（2026-09-14）**——把 `### GET /join/{token}` 整段移到 `DELETE /dept-invite-links/{token}` 的 `**说明**` 列表之后、`## 3.8` 之前。迁移后行号：DELETE 小节 `856-868`、其说明块 `870-874`、落地页段 `876-890`

**现象**（修复前）：新增的落地页小节落在了 `### DELETE /dept-invite-links/{token}` 的正文中间，
于是原本属于"招募链接"整体的设计说明被"抢"到了落地页标题下面，出现两个连续的 `**说明**：` 块。

**证据**（`docs/api-design.md`，行号为 `08db1fc`）

```
856: ### DELETE /dept-invite-links/{token}
858: **权限**：会长 / 副会长 —— 停用链接
860-868: > 语义是"停用"…  > 幂等…  > 响应形状统一…
870: ### GET /join/{token}（链接落地页，不在 /api/v1 下）      ← 插在这里
876: **说明**：                                                ← 落地页自己的说明
886: **说明**：                                                ← 其实是 DELETE 小节的说明
888: - 招募链接只做部门预填，不赋予任何角色…
889: - 链接不免除注册口令…
890: - 每个部门可以有多个链接…
892: ## 3.8 权限汇总表
```

**影响**：纯文档可读性问题。`888-890` 这三条讲的是整个招募链接功能的设计约束，
挂在 `GET /join` 标题下会让人以为它们在描述落地页。

**建议修法**：把 `### GET /join/{token}` 整段（`870-884`）移到 `DELETE` 小节的 `**说明**` 列表
（`886-890`）**之后**，即紧邻 `## 3.8` 之前。

### N-4 `README.md` 的冒烟计数写 `318`，其它三处都是 `325`

**状态**：**已修（2026-09-14）**——`README.md:38` 已改 `325`

**现象**（修复前）：`README.md:38` 写 `# HTTP 冒烟 318 项`，而我实跑是 **325**。
其它文档与第一轮报告都写 325，只有 README 这一行没同步。

**证据**

```
README.md:38           # HTTP 冒烟 318 项          ← 唯一不一致
HANDOFF.md:271         单测 273 / 冒烟 325 / TLS 22 全绿
server-guide.md:134    …（325 项）
我的实测               PASS 325 / FAIL 0
```

同一段的单测那行（`README.md:37` 的 273）是对的。

**建议修法**：`318` → `325`。顺带建议：这种"会随测试增长而失效的数字"集中放在一处
（例如只写在 README 的同一行与报告附录里），别在四处各写一遍。

### N-5 `idem` 表是 8 张表里唯一没走 `checkLoaded` 的

**状态**：**已修（2026-09-14）**——取方案 1：补上 `checkLoaded("idem", …)`，8 张表口径统一（没有留成"只存在于代码里的例外"）。单测加了一条"idem 坏记录也必须报错"，回退实测会变红（279 / 3）

**现象**：M-7 给 6 张表加了逐表条数比对，`idem` 没有；而 `checkLoaded` 的注释写着
"与文件级解析失败同一口径"，这里成了唯一的口径例外。

**证据**（`server/src/store.cj`）

```cangjie
344: checkLoaded("departments", deptArr, s.departments.size, file)
347: checkLoaded("members",     memberArr, s.members.size, file)
350: checkLoaded("plans",       planArr,   s.plans.size, file)
353: checkLoaded("tasks",       taskArr,   s.tasks.size, file)
356: checkLoaded("invite_links", linkArr,  s.invite_links.size, file)
359: checkLoaded("tokens",      tokenArr,  s.tokens.size, file)
360: s.idem = idemArrFromJson(r, "idem")        ← 没有 checkLoaded
```

**影响**：可忽略。`idem` 是 24 小时 TTL 的幂等映射，丢一条的最坏后果是
"客户端重试时多创建一个资源"，不会静默丢业务数据。

**建议修法**（二选一）

1. 补上 `checkLoaded("idem", idemArr, s.idem.size, file)`，保持 8 张表口径一致；
2. 保留现状，但在 `checkLoaded` 的注释里写明"`idem` 是短时效缓存，允许丢弃，故不校验"——
   **别让例外只存在于代码里**。

### N-6 注册口令节流是全局锁，可被用来长期挡住正常注册

**状态**：**已修（2026-09-14）**——按拍板结论实现为**按客户端 IP 计数 + 递增退避**，并**不新增接口**（见文末「实际修法」）。

**现象**（修复前）：M-2 的节流是**全局计数**。原报告把它归因于"轻舟没有可信客户端 IP"——
**这个前提本身是错的**（见下）。实测：连错 10 次后 `429`，**此时正确口令也被挡**。
文档已承认这是取舍，但还没写清它**可以被长期维持**。

**证据**

- `server/src/h_auth.cj` —— 全局计数与锁定：`reg_fail_count` / `reg_fail_first_at` / `reg_fail_until`
- 实测：连续错口令第 11 次出现 `429`；随后用**正确**口令注册 → 同样 `429`
- 计数窗口 15 分钟、锁定 15 分钟 → 攻击者每 15 分钟丢 10 个错口令即可维持封锁

**影响**：招新期是社团唯一大量注册的时段，而攻击者（或一个写错的客户端）能让注册入口
在这段时间内持续返回 `429`。属于**低成本、可持续的可用性干扰**，不涉及数据泄露。

**建议修法**（任选，建议 1 + 3）

1. 改成**递增退避**（首次锁 5 分钟，重复触发翻倍，上限 1 小时）—— 正常用户不会被坑太久，
   攻击者的封锁成本上升；
2. 或把阈值放宽（如 15 分钟 20 次）并只在"口令错误"上计数，减少误伤；
3. 补一条**自救路径**：会长在管理页能看到"注册当前被锁定至 X"，并可一键清除
   （现在只能等 15 分钟，或重启服务）；
4. 无论怎么改，`api-design` §2.3 注 6 要补一句"攻击者可用它持续封锁注册"。

**实际修法（2026-09-14）**

前提先被纠正：**"轻舟没有可信客户端 IP"是错的**。轻舟通过 `ctx.http.request.remoteAddr`
暴露 **TCP 对端地址**（`h_ops.cj` 的 `peerOf` 一直在用，`/admin/shutdown` 的"仅本机"就靠它），
那是传输层事实、客户端伪造不了；可伪造的是 `X-Forwarded-For` **请求头**，而本项目直连部署、不读它。
实测该字段文本为 `IP:端口`（例 `127.0.0.1:14764`）。

据此取「**按 IP + 递增退避**」，并且**不新增接口**：

1. **按 IP 分桶**：`Store.reg_fail` 从三个全局字段改成 `HashMap<IP, RegFail>`；新增
   `clientIpOf(ctx)` 从 `IP:端口` 剥出 IP（IPv6 取 `[...]` 内；无括号时按最后一个冒号切）。
2. **递增退避**：首次锁 5 分钟，同一 IP 每再触发一次翻倍，上限 1 小时。
3. **不再需要"会长自救入口"**：那是为对抗**全局**锁设计的（一锁就锁死所有人）。
   改成按 IP 之后，被锁的只有攻击者自己那个地址、正常用户不受影响，
   于是这个新接口**不必加，接口计数保持 39 / 39**。
4. 节流表加条数上限与过期回收（沿用 `pruneLoginFail` 的写法），防内存随 IP 无限增长。

**已知局限**（同时写进 `api-design` §2.3 注 6）：NAT / 校园网出口下整个社团可能共用一个公网 IP，
那时按 IP 会近似退化成全局锁；取不到对端地址时 key 为空串，同样退化为全局。
两者都比"无条件全局锁"更窄。

**回退实测**：把节流改回不分 IP → 单测 **287 / FAIL 5**（含核心的"另一个 IP 不受影响"）；
把锁定检查摘掉 → 冒烟 **336 / FAIL 2**（"锁定期间正确口令"实际返回 **201**，即锁完全失效）。

### N-7 "本接口不能对自己用"对**会长**不成立

**状态**：**已修（2026-09-14）**——取报告建议的"拦行为"路径，但**没有**在 handler 里写 `if`：约束落进 `perms.cj` 的 `checkAccess`（**动作语义层**），权限判定仍然只有一处。

按拍板结论，实现成一条**通用规则**而不是"只保护本人"的特例：**不可重置与自己同权或更高权的人**。
一次覆盖三种情况：

1. **对自己同权** → 谁都不能重置自己（这就是本条 N-7 的原始问题）；
2. **副会长对副会长同权** → 同权之间不能互相踢下线（**原先可以**，属本次一并堵住）；
3. **会长档位最高**，除自己外无人与之同权 → 仍能重置任何更低档的人（没有过度收紧）。

实现细节：这里**不能复用 `outranks()`** —— 它对 `president` 直接短路返回 `true`，会把"会长重置会长自己"放行，正是本条要拦的那一个；所以直接用 `roleRank(target) >= roleRank(actor)` 比较档位，另加一道"目标 id 就是操作者 id"的兜底（防 `target_role` 被填错）。

"本人改自己密码"的通道不变，仍是 `PUT /auth/password`（会长也一样）；只有"彻底忘记原密码"这个边界仍无自助找回（v1 范围取舍，见 `README.md` 未决事项 #4）。

回退实测：单测 **3 条变红**（279 / 3）、冒烟 **1 条变红**（333 / 1，同权重置实际返回 `200`）

**同日推广（2026-09-14，评审之外的一处主动收紧）**

上面只堵住了"重置密码"。继续推广时发现副会长档在 `roleAllows` 里对
`EditMemberName` / `SetRole` / `DisableMember` 也**只排除会长本人**，于是：

> **副会长可以把另一位副会长降级为 `member`、直接禁用、或改名。**

比重置密码更严重（直接改角色/状态）。已把同一条规则推广到这三类动作，
判定仍只在 `checkAccess` 的动作语义层。对自己**不**套用这条（改自己的姓名/角色/启停
属既有口径，且是自伤、无提权收益），只有"重置密码"连自己也禁（N-7 的原始理由）。

回退实测（撤回推广）：单测 **294 / FAIL 3**、冒烟 **340 / FAIL 3** ——
冒烟那三条实际全部返回 **200**，即推广前副会长确实能完成降级、禁用、改名。

**现象**（修复前）：`h_secret.cj` 的注释与 `api-design` §3.5 都写"本接口**不能**对自己用"，
但 `outranks` 对 `president` 直接短路返回 `true`，所以**会长可以重置自己**。

**证据**（`server/src/perms.cj`）

```cangjie
func outranks(actor_role: String, target_role: String): Bool {
    if (actor_role == "president") { return true }   // ← 会长不参与档位比较
    ...
}
```

**实测**

```
会长重置自己（临时密码）-> HTTP 200
会长的临时密码可登录     -> HTTP 200
```

行为本身无害（会长本就全能），但有两处值得写清楚：

1. 措辞不准确 —— 建议注释与文档改成"部长/副部长不能对自己用；会长不受档位限制"；
2. 这条路径会 `revokeOtherTokens(s, tgt.id, "")`，把会长**当前正在用的**令牌也一起废掉，
   表现为"响应 200，但自己的 token 立刻失效"。客户端要能处理这种"成功即掉线"，
   或者干脆在 handler 里对 `is_self` 直接拒绝（与文档口径一致，更简单）。

### N-8 `sortKeyClamp` 会把不同的越界值夹成同一档

**状态**：仅记录，**不建议改**

**现象**：`sortKeyClamp` 把 `> SORT_MAX` 的值统一夹到 `10000`。
旧库里若有两条部门 `sort` 分别溢出为 `Int64.Max` 与 `Int64.Max - 1`，clamp 后都是 `10000`，
两者退化为按 `id` 排序。

**影响**：无实际危害 —— 本来就是脏数据的兜底路径，目的是"别让接口 500"，而不是"还原正确顺序"。
记录在此，只为避免后人把它当成 bug 去改。

### N-9 落地页没有任何安全响应头

**状态**：**已修（2026-09-14）**——`handleJoinLanding` 加了一行 CSP（`default-src 'none'; style-src 'unsafe-inline'`），冒烟断言会读响应头校验（回退版实测 `csp=` 为空 → FAIL）

**现象**（修复前）：`handleJoinLanding` 只设置了 `content-type`，没有 CSP 等安全响应头。

**建议修法**：在 `handleJoinLanding` 的响应里加一行即可，成本极低且能顺带中和 N-1：

```cangjie
ctx.header("content-security-policy", "default-src 'none'; style-src 'unsafe-inline'")
```

（该页只有内联 `style` 属性，没有脚本与外部资源，所以 `default-src 'none'` 不会破坏页面显示。）

---

## 四、本轮未覆盖

| 项 | 说明 |
| --- | --- |
| 客户端 DevEco 构建 | `entry/` 仍是模板且本轮未改动，无法也无需构建验证（L-12 已如实记录） |
| 并发 / 压力 | L-7（全局锁横跨网络写出）保持"代码结构判断"的性质，本轮未做多客户端压测 |
| M-5 的行为观测 | `timeout` 移到外层后需要"慢接口"才能观测到 `x-timeout-ms` / 408；本轮只做了结构确认 |
| L-13 | 按约定保留不改 |

---

## 五、建议的收尾顺序

**第 1 批 —— 确定性文档修复（零风险，建议立刻做）· ✅ 2026-09-14 已完成**

> 三条全部完成，另同步了 `README.md:106` / `docs/HANDOFF.md:271` / `docs/server-guide.md:86`
> 三处评审口径（第 4 批第 9 项）。

1. **N-3** 把 `### GET /join/{token}` 移到 `DELETE /dept-invite-links/{token}` 之后
2. **N-4** `README.md:38` 的 `318` → `325` —— ✅
3. **N-2** 把"不要用双引号字符串改文本文件"补成 `API-NOTES` 第 27 条（防止再犯）—— ✅

**第 2 批 —— 落地页（新代码，建议先看 diff）· ✅ 2026-09-14 已完成**

4. **N-1** `joinPageHtml` 加 HTML 转义 —— ✅（落在 `strx.cj` 新增的 `htmlEscape`；逐字节实现，
   因为 `String.replace` 不在已实测清单里，而 `toArray()` 给的是 `Array<UInt8>`）
5. **N-9** `handleJoinLanding` 加 CSP 响应头 —— ✅

**第 3 批 —— 取舍与口径（需要先拍板）**

6. **N-6** 注册节流改递增退避 / 加会长自救入口，并在 `api-design` §2.3 注 6 补风险说明 —— ✅ **2026-09-14 已做**：改成**按客户端 IP + 递增退避**，因此**不需要**自救入口（也就没新增接口），注 6 已改写，并纠正了"没有可信客户端 IP"这个错误前提
7. **N-7** 措辞统一：要么改文档写"会长除外"，要么在 handler 里对 `is_self` 直接拒绝
   —— ✅ **2026-09-14 已做**（取后者，但约束放进 `checkAccess` 的动作语义层而不是 handler，
   以保持"权限判定只有一处"这条铁律；会长忘记密码的口径定为运维介入）
8. **N-5** `idem` 补 `checkLoaded`，或在注释里明确写成例外 —— ✅ **2026-09-14 已做**（取前者，8 张表口径统一）

**第 4 批 —— 措辞会过期的其它文件**

9. `README.md:106` / `docs/HANDOFF.md:271` / `docs/server-guide.md:86` 都把 `docs/code-review.md`
   描述成"3 个 P0 + 8 个 P1 + 13 个 P2"。本文件现在是**第二轮**报告（复验 + 9 条新发现），
   建议改成：
   > **代码评审报告（两轮）**：第一轮 24 条（3 P0 + 8 P1 + 13 P2）已全部修复并复验；
   > 第二轮记录复验证据与 9 条新发现。

---

## 附录 A · 复现命令

```powershell
# 基线：从提交源码重新编译，再跑三套测试
cd server
Remove-Item build\club-server.exe -Force -ErrorAction SilentlyContinue
powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
.\build\club-server.exe test                                              # 复验当时 PASS 273 / FAIL 0
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\smoke.ps1     # 复验当时 PASS 325 / FAIL 0
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\tls-check.ps1 # PASS 22  / FAIL 0

# 2026-09-14 处理完全部 9 条（N-1 / N-5 / N-6 / N-7 / N-9 等）后的**当前基线**：
#   单测 297 / 冒烟 343 / TLS 22 全绿（数字以 README.md 顶部那三行为准）

# 独立实例复现（每次用全新数据目录，绝不碰 server\data）
cd build
.\club-server.exe init-admin 13800000000 password123 verify-data
.\club-server.exe serve 18091 verify-data
# 之后按 §2.3 与各条 N-* 的请求重打
```

复现注意两条环境约束（`README.md` 的"六条最容易踩的坑"）：

- `cwd` 必须是 exe 所在目录（数据目录与证书按相对路径读）；
- 4 个 DLL 必须与 exe 同目录，缺两个 OpenSSL DLL 时**编译期无警告、运行时才报错**。

## 附录 B · 方法与一处环境误判说明

**方法**

- 所有"实测"结论都在本机跑过，数字（273 / 325 / 22 / `403` / `429` / 字节偏移 22337 等）均为实测值。
- 复验**不使用被复验方新增的测试作为证据**：先用第一轮报告里我自己的复现请求打一遍真实 HTTP，
  再回头核对他们的新断言是否真的能拦住回退。
- 涉及框架语义的结论（M-5 的中间件顺序、洋葱模型）读了轻舟源码 `compose.cj` / `router.cj` / `timeout.cj`。

**一处环境误判（必须说明，避免后人重踩）**

我最初两次跑 `tests/tls-check.ps1` 都得到 `PASS 3 / FAIL 3`，看起来像 TLS 被改坏了。
实际是本会话沙箱限制导致的**客户端假阴性**：

- `openssl` 每次启动即 `couldn't create signal pipe, Win32 error 5`（连续 5 次 0 成功，
  沙箱禁止 MSYS 程序创建 signal pipe）→ 两条 SAN 文本解析断言必失败，
  "错误 IP 必须失败"那条还会**假通过**（因为错误输出里本来就没有 `: OK`）；
- `curl.exe` 与 .NET `SslStream` 则被 Schannel 挡住（`No credentials are available in the security package`）。

我改用三条独立的旁证确认**服务端 TLS 没有问题**：

1. .NET 直接读证书，确认 SAN 完好：`IP Address=127.0.0.1, DNS Name=localhost, IP Address=::1`；
2. TCP 可连接，明文打 TLS 端口读回 0 字节（正确拒绝）；
3. 放开沙箱后重跑，`tls-check.ps1` → **PASS 22 / FAIL 0**（TLS 1.2/1.3 通过、1.0/1.1 被拒、
   真证书校验通过、错误 CA 失败、HTTPS 下登录闭环）。

顺带一条环境特性：**`Get-NetTCPConnection -State Listen` 在本环境会漏报正在监听的端口**
（服务确实在响应请求，它却说"没有监听"）。判断服务是否就绪，用真实请求或 TCP 连接，别信它。

## 附录 C · 文档修复清单（可照做）

| # | 文件 | 动作 |
| --- | --- | --- |
| 1 | `docs/api-design.md` | 把 `### GET /join/{token}` 整段（原 `870-884` 行）移到 `DELETE /dept-invite-links/{token}` 的 `**说明**` 列表之后、`## 3.8` 之前 |
| 2 | `README.md:38` | `# HTTP 冒烟 318 项` → `# HTTP 冒烟 325 项` |
| 3 | `docs/API-NOTES.md` | 新增第 27 条：不要用 PowerShell 双引号字符串处理 `.md` / `.cj` / `.ps1`（反引号 + `r`/`a`/`t`/`n` 会被当转义，静默吃掉反引号与首字母）；改完自检非法控制字符 |
| 4 | `README.md:106`、`docs/HANDOFF.md:271`、`docs/server-guide.md:86` | 把对 `docs/code-review.md` 的描述改为"两轮：第一轮 24 条已修复并复验，第二轮含复验证据与 9 条新发现" |
| 5 | `docs/api-design.md` §2.3 注 6 | 补一句"该节流是全局的，攻击者可用它持续封锁注册" |
| 6 | `server/src/h_secret.cj` 注释 + `docs/api-design.md` §3.5 | 措辞改为"部长/副部长不能对自己用；会长不受档位限制"（或在 handler 里对 `is_self` 直接拒绝，与文档对齐） |

> **进度（2026-09-14）：本轮 9 条全部处理完毕。**
> 第 **1 / 2 / 3 / 4** 项已完成（对应 N-3 / N-4 / N-2 + 三处评审口径同步）；
> 第 2 批的 **N-1 / N-9** 已完成（落地页转义 + CSP）；
> 第 6 项 **N-7** 已完成（约束落进 `checkAccess` 的动作语义层，实现为通用规则「不可重置同权 / 更高权的人」）；
> **N-5** 已完成（`idem` 补上 `checkLoaded`，8 张表口径统一）；
> **N-6** 已完成（注册节流改为**按客户端 IP + 递增退避** —— 因此**不需要**新增"会长自救入口"接口，
> 接口计数保持 **39 / 39**）；**N-8** 按约定不改。

## 附录 D · 第一轮报告在哪

| 版本 | commit | 内容 |
| --- | --- | --- |
| 原始版 | `ba95c36` | 24 条的完整证据、复现步骤、修复建议，以及"已排除的怀疑"（V-1～V-4）与"做得好的地方" |
| 带修复标注版 | `08db1fc` | 每条加了「状态：已修（2026-09-14）」+ 附录 C 回归证据 |

需要回看某条的原始证据时：

```powershell
git show 08db1fc:docs/code-review.md
```
